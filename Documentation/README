To install mkdocs : use the command lines below

For Ubuntu or Debian, you've have to install pip first.

pip install mkdocs
apt install python-pip
pip install --upgrade pip
pip install mkdocs
pip install mkdocs-material
